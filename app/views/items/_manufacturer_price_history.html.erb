<div class="card" id="manufacturer-list">
  <h4 class="card-header" style="text-decoration:underline;">Manufacturer Price</h4>
  <% unless @manufacturer_price_history.empty? %>
  <h5><strong>Current </strong><%= number_to_currency @item.current_manufacturer_price %></h5>
  <div class="card-block">
    <table class="ph table-hover">
      <thead>
        <tr class="ph-heading">
          <th>Price</th>
          <th>Start Date</th>
        </tr>
      </thead>
      <tbody>
        <% @manufacturer_price_history.each do |change| %>
          <tr>
            <td><%= number_to_currency change.price %></td>
            <td><%= change.start_date.strftime('%m/%d/%y') %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br />
  </div>
  <% else %>
  <p style="color: #ABB0BE;">No Manufacturer Price History</p>
  <% end %>
  <%= link_to(new_item_price_path(item_id: @item.id, category: 'manufacturer', id: 'new_manu_ip_link'), id: 'new_manu_ip_link', class: 'btn btn-primary', style: 'margin-top: 0px;', remote: true) do %>
    Add Manufacturer Price
  <% end %>
</div>
