<!-- <div class="row"> -->
  <!-- <div class="col-sm-2">
    <h3><%= @item.name %></h3>
  </div>
  <div class="col-sm-3">
    <%= image_tag 'img_not_available.png' %>
  </div> -->

<table class="item-show-body">
  <tr>
    <td class="item-show-img">
      <% unless @item.picture %>
        <%= link_to image_tag('placeholder.jpg', :alt => @item.name), item_path(@item) %>
      <% else %>
        <%= link_to image_tag(@item.picture, :alt => @item.name), item_path(@item) %>
      <% end %>
    </td>
    <td class="item-show-info" valign="top">
      <h3 class="item-show-name"><%= @item.name %></h3>
      <%= render :partial => "show_price" %>
      <p class="item-show-desc"><%= @item.description %></p>
      <button class="btn btn-primary add-to-cart"><span>ADD TO CART</span></button>

      <!-- <p>
        <b>Inventory Level</b>: <%= @item.inventory_level.nil? ? 'N/A' : @item.inventory_level %>
      </p>

      <p>
        <b>Reorder Level</b>: <%= @item.reorder_level.nil? ? 'N/A' : @item.reorder_level %>
      </p>

      <p>
        <b>Category</b>: <%= @item.category.capitalize %>
      </p>

      <p>
        <b>Color</b>: <%= @item.color.nil? ? 'N/A' : @item.color %>
      </p>

      <p>
        <b>Total weight</b>: <%= @item.weight.nil? ? 'N/A' : @item.weight %> lbs.
      </p>

      <p>
        <b>Active</b>: <%= @item.active ? 'Yes' : 'No' %>
      </p> -->
    </td>
  </tr>
</table>

<!-- </div> -->
