<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= favicon_link_tag %>
    <title>The Primary Chess Store - <%= controller.controller_name.capitalize %></title>
    <link href="https://fonts.googleapis.com/css?family=EB+Garamond|Josefin+Sans|Open+Sans:300,400,600,700,800" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%# debug params[:from] %>
    <div class="container-fluid">
    		<div class="row">
    			<div class="col-sm-1 sidebar">
    				<%= render :partial => "partials/nav" %>
    			</div>
    			<div class="col-sm-11 col-sm-push-1 main-signed-in">
            <%= render :partial => "partials/heading" %>
            <div id="under-heading-content">
              <%= yield %>
            </div>
    			</div>
    		</div>
    </div>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "nested_form" %>
  </body>
</html>
